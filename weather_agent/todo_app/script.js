const TodoService = { todos: [], add(item) { this.todos.push(item); }, remove(index) { this.todos.splice(index, 1); }, getAll() { return this.todos; } }; function render() { const list = document.getElementById("todoList"); list.innerHTML = ""; TodoService.getAll().forEach((t, i) => { const li = document.createElement("li"); li.textContent = t; const delBtn = document.createElement("button"); delBtn.textContent = "Delete"; delBtn.onclick = () => { TodoService.remove(i); render(); }; li.appendChild(delBtn); list.appendChild(li); }); } function addTodo() { const input = document.getElementById("todoInput"); if(input.value) { TodoService.add(input.value); input.value = ""; render(); } }
